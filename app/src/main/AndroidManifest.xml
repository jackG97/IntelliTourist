<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.intellitourist">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MuseumsPriceMapsActivity"
            android:label="@string/title_activity_museums_price_maps"></activity>
        <activity
            android:name=".MuesumsPriceMapsActivity"
            android:label="@string/title_activity_muesums_price" />
        <activity
            android:name=".MuseumsPriceMapsActvivty"
            android:label="@string/title_activity_museums_price_maps_actvivty" />
        <activity android:name=".MuseumsViewActivity" />
        <activity android:name=".BarsTypeMapsActivity" />
        <activity android:name=".BarsViewsActivity" />
        <activity android:name=".AgeActivity" />
        <activity android:name=".RestuarantsPopularityActivity" />
        <activity android:name=".RestuarantsPriceMapActivity" />
        <activity android:name=".RestaurantsViewsActivity" />
        <activity
            android:name=".MuseumsTypeMapsActivity"
            android:label="@string/title_activity_museums_maps" />
        <activity
            android:name=".ParksMapsActivity"
            android:label="@string/title_activity_parks_maps" />
        <activity
            android:name=".TouristPointsMapsActivity"
            android:label="@string/title_activity_tourist_points_maps" />
        <activity android:name=".GuidedActivity" />
        <activity
            android:name=".MoviesMapsActivity"
            android:label="@string/title_activity_movies_maps" />
        <activity
            android:name=".BarsPriceMapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SitesMapsActivity"
            android:label="@string/title_activity_sites_maps" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name=".RestuarantsMapActivity"
            android:label="@string/title_activity_map" />
        <activity android:name=".QuestionnaireActivity" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>