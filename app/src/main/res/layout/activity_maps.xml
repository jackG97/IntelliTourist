<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="530dp"
        tools:context=".MapsActivity" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:orientation="vertical">

        <Button
            android:id="@+id/nightclubsbtn"
            android:layout_width="424dp"
            android:layout_height="38dp"
            android:background="@drawable/button_background"
            android:onClick="onButtonClick"
            android:text="Not Show Nightclubs" />

        <Button
            android:id="@+id/nightclubsbtn1"
            android:layout_width="421dp"
            android:layout_height="36dp"
            android:background="@drawable/button_background"
            android:onClick="onButtonClick"
            android:text="Show Nightclubs" />

        <Button
            android:id="@+id/barsbtn"
            android:layout_width="match_parent"
            android:layout_height="36dp"
            android:background="@drawable/button_background"
            android:onClick="onButtonClick"
            android:text="Not show Bars" />

        <Button
            android:id="@+id/barsbtn1"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="@drawable/button_background"
            android:onClick="onButtonClick"
            android:text="Show Bars" />


    </LinearLayout>


</LinearLayout>